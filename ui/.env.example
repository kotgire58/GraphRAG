# Production (Render static site): set to your Web Service URL — https only, no trailing slash.
# Leave unset for local dev (Vite proxies /api to the API).
# VITE_API_BASE_URL=https://your-service.onrender.com
